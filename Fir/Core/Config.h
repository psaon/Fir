#ifndef __FIR_INCLUDE_CONFIG_H__
#define __FIR_INCLUDE_CONFIG_H__

#define FIR_SSO_MAX_CHARS 16

/* Configurations for the library:
 * - FIR_SSO_MAX_CHARS                          The maximum amount of characters that fit into a stack allocated string (Including the null terminator).
 *                                              Default Value: 16
 * 
 * - FIR_STRING_CUSTOM_ALLOCATOR                A custom allocator class that the string can use for allocating memory. (If specified)
 */

#endif        // Config.h